spring:
  datasource:
    url: jdbc:mysql://localhost:3306/glyphrz
    username: glyphrz
    password: hou812712~
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDB53Dialect
server:
  port: 32222